<!DOCTYPE html>
<html lang="ru">
  <head>
    <title><%= content_for(:title) || "Энва" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      body { background: #f5f5f5; font-family: 'Inter', sans-serif; -webkit-font-smoothing: antialiased; font-feature-settings: "tnum" on, "lnum" on; }
    </style>
  </head>

  <body class="min-h-screen flex flex-col">

    <nav class="bg-white border-b border-[#e5e5e5] h-14 flex items-center px-6 shrink-0">
      <div class="flex items-center gap-8 w-full">

        <a href="<%= enva_root_path %>" class="flex items-center gap-2">
          <div class="w-7 h-7 bg-[#0062FF] rounded-lg flex items-center justify-center">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8h10M8 3v10" stroke="white" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </div>
          <span class="text-[15px] font-semibold text-[#1a1a1a] tracking-tight">Энва</span>
        </a>

        <div class="flex items-center gap-1">
          <%= link_to "Мои сервисы", enva_root_path,
            class: "px-3 py-1.5 rounded-lg text-sm font-medium transition-colors #{current_page?(enva_root_path) ? 'bg-[#f0f4ff] text-[#0062FF]' : 'text-[#404040] hover:bg-[#f5f5f5]'}" %>
        </div>

        <div class="ml-auto flex items-center gap-3">
          <a href="<%= new_enva_service_path %>" style="display:inline-flex;align-items:center;gap:6px;background:#0062FF;color:white;font-size:14px;font-weight:500;padding:0 16px;height:36px;border-radius:8px;border:none;cursor:pointer;text-decoration:none;">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
              <path d="M7 1v12M1 7h12" stroke="white" stroke-width="1.8" stroke-linecap="round"/>
            </svg>
            Новый сервис
          </a>
          <div class="w-8 h-8 rounded-full bg-[#0062FF] flex items-center justify-center text-white text-xs font-semibold select-none">
            АН
          </div>
        </div>

      </div>
    </nav>

    <div class="flex-1">
      <%= yield %>
    </div>

  </body>
</html>
