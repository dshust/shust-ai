<%# Шапка — тёмная полоса на всю ширину %>
<header class="bg-[#1a1a1a] text-white">
  <div class="max-w-6xl mx-auto px-6 py-10 flex items-end justify-between gap-6 flex-wrap">
    <div>
      <div class="text-xs font-semibold uppercase tracking-widest text-[#888888] mb-2">Портфолио</div>
      <h1 class="text-3xl sm:text-4xl font-extrabold tracking-tight leading-none">Дмитрий Шустов</h1>
      <p class="mt-3 text-[#aaaaaa] text-base max-w-md leading-relaxed">
        Делаю полезные сервисы на стыке AI и продукта.
      </p>
    </div>
    <div class="flex gap-4 text-sm pb-1">
      <a href="https://t.me/dvshustov" target="_blank"
         class="inline-flex items-center gap-1.5 bg-[#2e2e2e] hover:bg-[#3a3a3a] transition-colors px-4 py-2 rounded-full font-medium text-white">
        Telegram
      </a>
      <a href="https://github.com/dshust" target="_blank"
         class="inline-flex items-center gap-1.5 bg-[#2e2e2e] hover:bg-[#3a3a3a] transition-colors px-4 py-2 rounded-full font-medium text-white">
        GitHub
      </a>
    </div>
  </div>
</header>

<%# Основной контент %>
<main class="max-w-6xl mx-auto px-6 py-10">

  <%# Сетка проектов %>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
    <% @projects.each do |project| %>
      <a href="<%= project.url %>" target="_blank" class="group block bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-md transition-shadow duration-200">

        <%# Цветной блок с эмодзи %>
        <div class="bg-[#f0f0f0] group-hover:bg-[#e8e8e8] transition-colors flex items-center justify-center h-36">
          <span class="text-7xl leading-none select-none"><%= project.emoji %></span>
        </div>

        <%# Текстовая часть %>
        <div class="p-5">
          <div class="text-[10px] font-semibold uppercase tracking-widest text-[#999999] mb-1.5">
            <%= project.tag %>
          </div>
          <h3 class="text-base font-bold text-[#1a1a1a] leading-snug mb-2 group-hover:text-[#444444] transition-colors">
            <%= project.name %>
          </h3>
          <p class="text-sm text-[#666666] leading-relaxed">
            <%= project.description %>
          </p>
          <div class="mt-4 text-xs text-[#bbbbbb] font-medium">
            <%= project.url.gsub(/^https?:\/\//, '').gsub(/\/$/, '') %> ↗
          </div>
        </div>

      </a>
    <% end %>
  </div>

</main>

<footer class="max-w-6xl mx-auto px-6 pb-10 text-xs text-[#bbbbbb]">
  shust.ai
</footer>
